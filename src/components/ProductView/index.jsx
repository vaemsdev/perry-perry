import React from 'react';
import styles from './ProductView.module.scss';

const ProductView = () => (
  <div className={styles.ProductView} data-testid="ProductView">
    <h1>ProductView component</h1>
  </div>
);

export default ProductView;